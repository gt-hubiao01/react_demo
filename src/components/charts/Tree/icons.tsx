export const plusPath = (radius: number) =>
  `M ${-radius / 2} 0 H ${radius / 2} M 0 ${-radius / 2} V ${radius / 2}`;
export const minusPath = (radius: number) =>
  `M ${-radius / 2} 0 H ${radius / 2}`;
